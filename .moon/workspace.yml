# https://moonrepo.dev/docs/config/workspace
$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  web: 'apps/web'
  server: 'apps/server'

vcs:
  provider: 'github'
  defaultBranch: 'main'
