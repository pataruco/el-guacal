query GetStoreById($storeId: UUID!) {
  getStoreById(id: $storeId) {
    name
    storeId
    address
    updatedAt
    location {
      lat
      lng
    }
    products {
      name
      productId
    }
  }
}
