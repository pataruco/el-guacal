query GetStoreById($storeId: UUID!) {
  getStoreById(id: $storeId) {
    name
    storeId
    address
    version
    updatedAt
    location {
      lat
      lng
    }
    products {
      name
      productId
    }
  }
}
