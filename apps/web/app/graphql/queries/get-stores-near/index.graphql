query GetStoresNear(
  $location: LocationInput!
  $radius: Radius!
  $productIds: [UUID!]
) {
  storesNear(location: $location, radius: $radius, productIds: $productIds) {
    name
    storeId
    location {
      lat
      lng
    }
    products {
      name
    }
  }
}
